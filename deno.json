{
  "tasks": {
    "dev": "ENV=development deno run --watch --import-map=import_map.json --allow-net --allow-read --allow-env --allow-sys src/index.ts",
    "test": "ENV=test deno test --import-map=import_map.json --allow-net --allow-read --allow-env --allow-sys tests"
  }
}
